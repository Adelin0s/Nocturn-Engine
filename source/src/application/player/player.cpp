#include "application/player/player.h"

namespace Nocturn
{
    void Player::update(const float dt)
    {
        // m_rigidbody.update(dt);
        // updateCameraPosition(m_rigidbody.getPosition());
    }
}